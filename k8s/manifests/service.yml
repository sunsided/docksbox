apiVersion: v1
kind: Service
metadata:
  name: docksbox
  labels:
    app: docksbox
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: docksbox
